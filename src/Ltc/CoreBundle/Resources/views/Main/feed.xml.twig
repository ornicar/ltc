<feed xmlns="http://www.w3.org/2005/Atom">
    <title>Les Trois Couronnes</title>
    <subtitle>Didactique de l'information-documentation et identité professionnelle de l'enseignant documentaliste. Publications de Pascal Duplessis</subtitle>
    <id>{{ url("homepage") }}</id>
    <link href="{{ url("homepage") }}" rel="alternate" />
    <link href="{{ url("homepage") }}" />
    <updated>{{ objects[0].publishedAt|date("c") }}</updated>
    {% for obj in objects %}
    <entry>
        <title>{{ obj.title }}</title>
        {% if obj.isDoc %}
            <author>
                <name>{{ ltc_doc_author(obj) }}</name>
            </author>
            <updated>{{ obj.publishedAt|date("c")|raw }}</updated>
            <id>{{ ltc_doc_url(obj) }}</id>
            <link rel="alternate">{{ ltc_doc_url(obj) }}</link>
            <summary>{{ obj.summary }}</summary>
        {% else %}
            <author>
                <name>{{ obj.authorName }}</name>
            </author>
            <updated>{{ obj.publishedAt|date("c")|raw }}</updated>
            <id>{{ path('ltc_story_view', {"slug": obj.slug}) }}</id>
            <link rel="alternate">{{ path('ltc_story_view', {"slug": obj.slug}) }}</link>
            <summary>{{ obj.summary }}</summary>
        {% endif %}
    </entry>
    {% endfor %}
</feed>
