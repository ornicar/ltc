<a title="Fil RSS des discussions" class="comment_rss_link" href="{{ path("ltc_comment_feed") }}">
    <img alt="Flux RSS" src="/bundles/ltccore/img/rss_icon.jpg?15">
</a>
<a class="title" href="{{ path('ltc_comment_list') }}">Discussions</span>
<ul class="recent_comments">
    {% for comment in comments %}
    <li>
        <a href="{{ comment.url }}">{{ comment.authorName }}</a>
        <span class="body">{{ comment.body|ltc_shorten }}</span>
        <a href="{{ comment.url }}">[...]</a>
    </li>
    {% endfor %}
</ul>
<a class="all_others" href="{{ path('ltc_comment_list') }}">&gt; autres discussions</a>
